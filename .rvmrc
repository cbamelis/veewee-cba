#!/bin/bash
RUBY_VERSION=1.9.3
test -Z ${JENKINS_URL} && JOB_NAME=veewee-cba
GEMSET=${JOB_NAME}
rvm use ${RUBY_VERSION} || rvm install ${RUBY_VERSION}
rvm use ${RUBY_VERSION}@${GEMSET} --create
bundle || gem install bundler
bundle check || bundle install --binstubs
export PATH=./bin:${PATH}
